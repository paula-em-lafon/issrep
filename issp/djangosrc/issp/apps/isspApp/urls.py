# __author__ = 'user'

#from django.conf.urls import patterns,url

#urlpatterns = patterns('issp.apps.isspApp.views',
                       #url=(r'^$','index_view', name="index"),
                       #)

# from django.conf.urls import patterns, url

# urlpatterns = patterns("issp.apps.isspApp.views",
#                        url(r'^$', 'index_view', name="index"),
#                        url(r'^$', 'information_view', name="information")
#                        )
